% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_materialize.R
\name{cb_materialize}
\alias{cb_materialize}
\title{Materialize a burn as a dense raster}
\usage{
cb_materialize(x, value = c("coverage", "id", "count"), background = NA)
}
\arguments{
\item{x}{A \code{cb_burn} object.}

\item{value}{What each cell should hold:
\describe{
\item{\code{"coverage"}}{(default) summed coverage. Interior run cells
contribute 1, boundary cells their \code{fraction}, line cells their
\code{length} (CRS units), points 1 each. Overlapping geometries add, so
values above 1 are possible; use \code{pmin(., 1)} if a clamped mask is
wanted. Output \code{datatype} is \code{"Float32"}.}
\item{\code{"id"}}{the 1-based input position of the geometry covering the
cell; where geometries overlap the highest \code{id} wins (later inputs
are burned over earlier ones). Boundary, line, and point cells count
as covered. Output \code{datatype} is \code{"Int32"}.}
\item{\code{"count"}}{the number of distinct geometries touching the cell.
Output \code{datatype} is \code{"Int32"}.}
}}

\item{background}{Value for cells not touched by any geometry. Default \code{NA}.}
}
\value{
A numeric (\code{"coverage"}) or integer (\code{"id"}, \code{"count"}) vector of
length \code{ncol * nrow} with attribute \code{gis}, a list:
\code{type = "raster"}, \code{bbox = c(xmin, ymin, xmax, ymax)},
\code{dim = c(ncol, nrow, 1)}, \code{srs} (the burn's \code{crs}, or \code{""}), and
\code{datatype}.
}
\description{
Expand the sparse tables of a \code{\link[=cb_burn]{cb_burn()}} into a dense cell vector in the
layout returned by \code{gdalraster::read_ds()}: a plain vector, one element per
cell, in row-major order starting at the top-left cell, carrying a \code{"gis"}
attribute that describes the grid (\code{type}, \code{bbox}, \code{dim}, \code{srs},
\code{datatype}). This is a stable format definition, so no dependency on
'gdalraster' is needed to produce or consume it, and the result can be
handed straight to \code{gdalraster::plot_raster()} or wrapped as a matrix with
\code{\link[=cb_as_matrix]{cb_as_matrix()}}.
}
\details{
Consider \code{\link[=cb_crop]{cb_crop()}} first when the parent grid is large and the geometry
occupies only part of it.
}
\examples{
\dontrun{
library(wk)
sq <- wkt("POLYGON ((2.5 4.5, 6.5 4.5, 6.5 8.5, 2.5 8.5, 2.5 4.5))")
b <- cb_burn(sq, extent = c(0, 10, 0, 10), dimension = c(10, 10))
v <- cb_materialize(b)
cb_as_matrix(v)
}
}
