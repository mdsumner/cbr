% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_summary.R
\name{summary.cb_burn}
\alias{summary.cb_burn}
\title{Summarize a burn without materializing it}
\usage{
\method{summary}{cb_burn}(object, tiles = 256L, ...)
}
\arguments{
\item{object}{A \code{cb_burn} object.}

\item{tiles}{Target number of tiles along the longer grid axis for the
overview. The tile size in cells is \code{ceiling(max(dimension) / tiles)}.}

\item{...}{Ignored.}
}
\value{
A list of class \code{summary.cb_burn}.
}
\description{
Compute a compact description of a \code{\link[=cb_burn]{cb_burn()}} object in time proportional
to the number of records in its tables, never to the number of grid cells.
The pieces are:
}
\details{
\describe{
\item{\code{overview}}{a coarse raster of mean coverage per tile, in the same
\code{read_ds} layout as \code{\link[=cb_materialize]{cb_materialize()}} (see \code{\link[=cb_overview]{cb_overview()}}). When the
grid is small enough that one tile is one cell, this is the exact
coverage.}
\item{\code{row_marginal}, \code{col_marginal}}{exact covered-cell totals per grid
row and per grid column, at full resolution.}
\item{\code{content}}{the extent and 1-based row/col range of touched cells, or
\code{NULL} for an empty burn. \code{\link[=cb_crop]{cb_crop()}} uses this as its default window.}
\item{\code{area}}{per-geometry totals: \code{interior} (run cells), \code{boundary}
(summed edge fractions), \code{lines} (summed length), \code{points} (count).}
\item{\code{fractions}}{quantiles of the edge coverage fractions.}
\item{\code{records}}{row counts of the five tables.}
}

Coverage here means: run cells 1 each, edge cells their fraction, line and
point cells 1 each (presence).
}
