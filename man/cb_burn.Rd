% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_burn.R
\name{cb_burn}
\alias{cb_burn}
\title{Burn geometries onto a regular grid}
\usage{
cb_burn(geom, extent, dimension, coverage = TRUE, crs = NA_character_)
}
\arguments{
\item{geom}{Geometry input. Anything that \code{\link[wk:as_wkb]{wk::as_wkb()}} understands, e.g. a
\code{wk_wkb} vector, an \code{sf}/\code{sfc} column, or a list of raw WKB blobs.}

\item{extent}{Grid extent as \code{c(xmin, xmax, ymin, ymax)} (the ordering used
by \code{terra::ext()}). If missing, the bounding box of \code{geom} is used (via
\code{\link[wk:wk_bbox]{wk::wk_bbox()}}).}

\item{dimension}{Grid dimensions as \code{c(ncol, nrow)}. If missing, \code{ncol} is
256 and \code{nrow} is computed from it so that cells are square in the units
of \code{extent} (at least 1 row). A single value is taken as \code{ncol}, with
\code{nrow} computed the same way.}

\item{coverage}{Logical. If \code{TRUE} (default) use exact coverage fractions
(boundary cells appear in \code{edges}). If \code{FALSE} use the faster cell-centre
("approx", fasterize-style) rule, which emits runs only.}

\item{crs}{Optional coordinate reference system as a WKT string (or anything
coercible to character). Carried through to \code{\link[=cb_materialize]{cb_materialize()}} output.}
}
\value{
An object of class \code{cb_burn}: a named list of five data frames with
the grid definition stored in attributes (\code{extent}, \code{dimension}, \code{crs},
\code{coverage}) and the number of input geometries in \code{n_geom}. Indices are 1-based and row 1 is the top row; geometry \code{k}
(input position) gets \code{id = k}.
\describe{
\item{\code{runs}}{\code{row}, \code{col_start}, \code{col_end}, \code{id}: fully covered cells.}
\item{\code{edges}}{\code{row}, \code{col}, \code{fraction}, \code{id}: boundary cells, fraction in (0, 1).}
\item{\code{lines}}{\code{row}, \code{col}, \code{length}, \code{id}: line length within the cell, CRS units.}
\item{\code{points}}{\code{row}, \code{col}, \code{id}: one row per point inside the grid.}
\item{\code{notes}}{\code{geom_index}, \code{message}: non-fatal per-geometry problems.}
}
}
\description{
Rasterize polygons, lines, and points onto a regular grid using the
\code{controlledburn} Rust crate. Polygon interiors are returned as horizontal
runs of fully covered cells, and (in coverage mode) boundary cells are
returned with their exact coverage fraction.
}
\examples{
\dontrun{
library(wk)
sq <- wkt("POLYGON ((2.5 4.5, 6.5 4.5, 6.5 8.5, 2.5 8.5, 2.5 4.5))")
cb_burn(sq, extent = c(0, 10, 0, 10), dimension = c(10, 10))

## defaults: bbox of the geometry, 256 columns, square cells
cb_burn(sq)
}
}
