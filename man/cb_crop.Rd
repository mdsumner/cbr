% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_crop.R
\name{cb_crop}
\alias{cb_crop}
\title{Crop a burn to a sub-window of its grid}
\usage{
cb_crop(x, extent, snap = c("out", "in", "near"))
}
\arguments{
\item{x}{A \code{cb_burn} object.}

\item{extent}{Crop extent as \code{c(xmin, xmax, ymin, ymax)}, in the CRS of the
grid. It is snapped to the parent grid and intersected with it.}

\item{snap}{How to align \code{extent} to cell boundaries. \code{"out"} (default)
expands to the enclosing cells, \code{"in"} shrinks to fully contained cells,
\code{"near"} moves each edge to the nearest cell boundary.}
}
\value{
A \code{cb_burn} object on the cropped grid. The \code{extent} and \code{dimension}
attributes describe the new grid; \code{crs} and \code{coverage} are carried through.
The parent grid is recorded in attribute \code{parent} as a list with
\code{extent}, \code{dimension}, and the 0-based cell \code{offset} (\code{c(col, row)}) of
the window within it. An error is raised if the snapped window is empty.
}
\description{
Snap an arbitrary extent to the cell boundaries of a \code{\link[=cb_burn]{cb_burn()}} grid and
subset the burn tables to that window. This is a pure index operation: no
geometry is re-rasterized, runs that straddle the window edge are clipped,
and row/column indices are re-based so that the result is a valid \code{cb_burn}
on the smaller grid. Use it before \code{\link[=cb_materialize]{cb_materialize()}} to avoid allocating a
dense array for the whole parent grid.
}
\examples{
\dontrun{
library(wk)
sq <- wkt("POLYGON ((2.5 4.5, 6.5 4.5, 6.5 8.5, 2.5 8.5, 2.5 4.5))")
b <- cb_burn(sq, extent = c(0, 10, 0, 10), dimension = c(10, 10))
cb_crop(b, c(2.2, 5.1, 4.9, 9))   # snapped to x[2, 6] y[4, 9]
}
}
