% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_summary.R
\name{cb_overview}
\alias{cb_overview}
\title{Coarse overview raster of a burn}
\usage{
cb_overview(x, tiles = 256L, block = NULL)
}
\arguments{
\item{x}{A \code{cb_burn} object.}

\item{tiles}{Target number of tiles along the longer axis; ignored if
\code{block} is given.}

\item{block}{Tile size in cells. \code{block = 1} gives the exact coverage.}
}
\value{
A numeric vector with a \code{gis} attribute (\code{datatype = "Float32"}),
and additionally attribute \code{block} giving the tile size used. Untouched
tiles are \code{NA}.
}
\description{
Aggregate a \code{\link[=cb_burn]{cb_burn()}} onto a grid of square tiles of \code{block} cells and
return the mean coverage per tile (in \verb{[0, 1]} for polygons) in the
\code{read_ds} layout used by \code{\link[=cb_materialize]{cb_materialize()}}. Edge tiles that are cut off by
the grid boundary are normalized by their actual cell count. Cost is
proportional to the number of records plus the number of tiles: a run that
spans several tiles contributes partial lengths to its two end tiles and the
fully covered tiles between them are filled with a difference array, so
nothing is ever expanded to cells whatever the block size.
}
