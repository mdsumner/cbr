% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cb_plot.R
\name{plot.cb_burn}
\alias{plot.cb_burn}
\title{Plot a burn}
\usage{
\method{plot}{cb_burn}(
  x,
  what = c("auto", "exact", "overview"),
  marginals = TRUE,
  col = grDevices::hcl.colors(64, "YlOrRd", rev = TRUE),
  by_id = FALSE,
  tiles = 256L,
  threshold = 50000,
  content = FALSE,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cb_burn} object.}

\item{what}{\code{"auto"}, \code{"exact"}, or \code{"overview"}.}

\item{marginals}{Draw the row and column marginal strips. The layout is
sized from the extent's aspect ratio so the strips span exactly the map;
leftover device space is left blank.}

\item{col}{Colour ramp (a vector of colours, low to high coverage) for
\code{"overview"} and for edge shading in \code{"exact"}.}

\item{by_id}{In \code{"exact"} mode, colour runs and edges by geometry \code{id}
instead of by coverage.}

\item{tiles}{Passed to \code{\link[=cb_overview]{cb_overview()}} for the \code{"overview"} rendering.}

\item{threshold}{Record count above which \code{"auto"} switches to \code{"overview"}.}

\item{content}{Draw the content extent (bounding box of touched cells) as a
dashed rectangle.}

\item{main}{Optional title, drawn in the outer margin above the strips.}

\item{...}{Passed to \code{\link[graphics:plot]{graphics::plot()}} for the main panel.}
}
\value{
\code{x}, invisibly. The summary computed for the marginals is attached
as attribute \code{summary}.
}
\description{
Draw a \code{\link[=cb_burn]{cb_burn()}} in map coordinates (\code{asp = 1}) with the grid extent as
the frame. Two renderings are available:
}
\details{
\describe{
\item{\code{"exact"}}{every record is drawn: runs as rectangles, edge cells as
rectangles shaded by coverage fraction, line cells shaded by length,
points as cells. Cost is one rectangle per record, so this is the
picture of the sparse representation itself.}
\item{\code{"overview"}}{the coarse mean-coverage raster from \code{\link[=cb_overview]{cb_overview()}}
drawn with \code{\link[graphics:image]{graphics::image()}}. Cost does not depend on grid size.}
}

\code{"auto"} (default) picks \code{"exact"} when the total record count is at most
\code{threshold}, else \code{"overview"}. Either way the optional marginal strips are
exact: covered cells per grid row (right) and per grid column (top), from
\code{\link[=summary.cb_burn]{summary.cb_burn()}}.
}
